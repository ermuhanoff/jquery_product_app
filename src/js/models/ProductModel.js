export default class ProductModel {
  constructor({ name, count, price, email, delivery }) {
    this.name = name;
    this.count = count;
    this.price = price;
    this.email = email;
    this.delivery = delivery;
  }
}
